setup = {
pinMode(7, OUTPUT);
pinMode(6, OUTPUT);
pinMode(5,OUTPUT);
}

loop = {
after 1sec in timerA do {
toggle(7);
resetTimer;
}

after 2sec in timerB do{
toggle(6);
resetTimer;
}
after 3sec in timerC do{
toggle(5);
resetTimer;

}
}

