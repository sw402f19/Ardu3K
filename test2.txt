setup = {
    pinMode(8, OUTPUT);
    pinMode(analog3, OUTPUT);
}

loop = {
    after 1sec in timerA do {
        toggle(8);
        reset;
    }

    after 3sec in timerB do {
        toggle(analog3);
        reset;
    }
}

