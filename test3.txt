setup = {
   pinMode(1, OUTPUT);
}
loop = {
    ClockY = 10;
   after 1sec in ClockY do {
       toggle(analog2);
       resetTimer;
       before 1min in TimerT do resetTimer;
       resetTimer;
   }
   before 1min in TimerT do resetTimer;
}
