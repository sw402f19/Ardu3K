setup = {
    kage = 2;
}

loop = {
    // HELLO //
    before 10sec in kage do {
        toggle(analog2);
        reset(kage);
    }
    after 10sec in kage do {
        toggle(analog2);
        reset(kage);
    }
}

