setup = {
   pinMode(analog0, OUTPUT);
   pinMode(analog3, OUTPUT);
}
loop = {
   after 1sec in timer do resetTimer;
   after 1min in timerZ do {
        toggle(analog3);
        resetTimer;
   }
   for 3  to 5 do toggle(2);
}
