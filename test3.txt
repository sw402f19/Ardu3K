setup = {
}

loop = {
    // HELLO //
    before 10sec in kage do {
        toggle(analog2);
        reset(hest);
        reset;
    }
    after 10sec in hest do {
        toggle(analog2);
        read(1);
        write(2, false);
        pinMode(2, OUTPUT);
        reset(this);
    }
}

